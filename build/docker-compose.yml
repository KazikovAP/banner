version: '3'

services:
  banner-service:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "6432:5432"
    volumes:
      - '../migrations/:/docker-entrypoint-initdb.d/'